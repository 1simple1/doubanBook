<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.simplem.doubanbook.mapper.CartMapper">

    <update id="setPrise">
        update cart set totalPrice = totalPrice + #{price} where id = #{cid}
    </update>

    <select id="selectBooks" parameterType="int" resultType="int">
        select bookId  from cart_book where cartId = #{cid}
    </select>
</mapper>